ALTER TABLE `washington` ADD `result` VARCHAR(100) NULL AFTER `outcome`, ADD `other_result` VARCHAR(100) NULL AFTER `result`, ADD `lhj_species` VARCHAR(100) NULL AFTER `other_result`, ADD `lhj_serotype` VARCHAR(100) NULL AFTER `lhj_species`, ADD `case_name` VARCHAR(100) NULL AFTER `lhj_serotype`, ADD `days_from_onset` VARCHAR(100) NULL AFTER `case_name`, ADD `calendar_dates` VARCHAR(100) NULL AFTER `days_from_onset`;
ALTER TABLE `washington` ADD `travel_area` VARCHAR(100) NULL AFTER `calendar_dates`, ADD `travel_state` VARCHAR(100) NULL AFTER `travel_area`, ADD `out_of` VARCHAR(100) NULL AFTER `travel_state`, ADD `dates_locations` VARCHAR(100) NULL AFTER `out_of`, ADD `number_people` VARCHAR(100) NULL AFTER `dates_locations`, ADD `number_people_text` VARCHAR(100) NULL AFTER `number_people`, ADD `contact` VARCHAR(100) NULL AFTER `number_people_text`, ADD `health_care` VARCHAR(100) NULL AFTER `contact`;
ALTER TABLE `washington` ADD `patient_interviewed` TINYINT(1) NULL AFTER `health_care`, ADD `no_risk` TINYINT(1) NULL AFTER `patient_interviewed`, ADD `probably_occur` VARCHAR(100) NULL AFTER `no_risk`, ADD `probably_occur_country` VARCHAR(100) NULL AFTER `probably_occur`, ADD `exposure_details` VARCHAR(100) NULL AFTER `probably_occur_country`, ADD `health_care_setting` VARCHAR(100) NULL AFTER `exposure_details`;
ALTER TABLE `washington` ADD `health_exposure` VARCHAR(100) NULL AFTER `health_care_setting`, ADD `health_setting` VARCHAR(100) NULL AFTER `health_exposure`, ADD `health_setting_other` VARCHAR(100) NULL AFTER `health_setting`, ADD `congregate` VARCHAR(100) NULL AFTER `health_setting_other`, ADD `congregate_living` VARCHAR(100) NULL AFTER `congregate`, ADD `congregate_living_other` VARCHAR(100) NULL AFTER `congregate_living`, ADD `poultry` VARCHAR(100) NULL AFTER `congregate_living_other`, ADD `poultry_type` VARCHAR(100) NULL AFTER `poultry`, ADD `poultry_type_other` VARCHAR(100) NULL AFTER `poultry_type`, ADD `poultry_animals` VARCHAR(100) NULL AFTER `poultry_type_other`;
ALTER TABLE `washington` ADD `poultry_description` VARCHAR(100) NULL AFTER `poultry_animals`, ADD `nosocomial` VARCHAR(100) NULL AFTER `poultry_description`, ADD `work` VARCHAR(100) NULL AFTER `nosocomial`, ADD `facility_name` VARCHAR(100) NULL AFTER `work`, ADD `close_contact` VARCHAR(100) NULL AFTER `facility_name`, ADD `outbreak` TINYINT(1) NULL AFTER `close_contact`;
ALTER TABLE `washington` ADD `home_isolation` TINYINT(1) NULL AFTER `outbreak`, ADD `home_isolation_date` VARCHAR(100) NULL AFTER `home_isolation`, ADD `contact_quarantine` TINYINT(1) NULL AFTER `home_isolation_date`, ADD `contact_number` VARCHAR(100) NULL AFTER `contact_quarantine`, ADD `facility_notified` TINYINT(1) NULL AFTER `contact_number`;
ALTER TABLE `washington` ADD `notes` VARCHAR(100) NULL AFTER `facility_notified`, ADD `OPTIONAL_TRAVEL` INT(11) NULL AFTER `notes`, ADD `OPTIONAL_HOUSEHOLD` INT(11) NULL AFTER `optional_travel`, ADD `investigator` VARCHAR(100) NULL AFTER `optional_household`, ADD `investigator_phone` VARCHAR(100) NULL AFTER `investigator`, ADD `local_health` VARCHAR(100) NULL AFTER `investigator_phone`, ADD `complete_date` VARCHAR(100) NULL AFTER `local_health`;
ALTER TABLE `washington` ADD `record_date` VARCHAR(100) NULL AFTER `complete_date`;